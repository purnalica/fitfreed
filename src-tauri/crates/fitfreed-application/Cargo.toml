[package]
name = "fitfreed-application"
version = "0.1.0"
edition = "2021"
description = "FitFreed application use cases and ports"
license = "GPL-3.0-or-later"
repository = "https://github.com/purnalica/fitfreed"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
fitfreed-domain = { path = "../fitfreed-domain" }
semver = "1"
thiserror = "2"
